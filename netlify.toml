# React (Vite) build — migrated from monolith public/index.html
[build]
  command = "npm run build"
  publish = "dist"

[[headers]]
  for = "/*"
  [headers.values]
    Permissions-Policy = "geolocation=(self)"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

