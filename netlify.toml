# React (Vite) build
[build]
  command = "npm run build"
  publish = "dist"

[[headers]]
  for = "/*"
  [headers.values]
    Permissions-Policy = "geolocation=(self)"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
