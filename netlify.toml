# React (Vite) build — with static blog pre-rendering
[build]
  command = "node build-blog.js && npm run build"
  publish = "dist"

[[headers]]
  for = "/*"
  [headers.values]
    Permissions-Policy = "geolocation=(self)"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
